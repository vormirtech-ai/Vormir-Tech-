package com.lavidhawa.pos;

import android.app.Application;

/**
 * Nothing to set up at process start — the billing system lives entirely in
 * the WebView. The class exists so the manifest can name a stable entry point.
 */
public class App extends Application {
}
