# MedV needs its devDependencies (electron, electron-builder) to build the
# Windows installer. Some machines have NODE_ENV=production or
# `npm config set production true` set globally, which silently skips them and
# leaves you with "'electron-builder' is not recognized". This makes the build
# work regardless of that.
include=dev
